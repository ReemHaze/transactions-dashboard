# Base URL of your backend API. Leave as /api if you're proxying
# through Vite (see vite.config.js) or serving frontend+API from the
# same origin. Point it at a full URL if your API lives elsewhere, e.g.:
# VITE_API_BASE_URL=https://api.yourcompany.com
VITE_API_BASE_URL=/api
